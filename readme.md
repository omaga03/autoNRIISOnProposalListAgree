# autoNRIISOnProposalListAgree_Omaga03

ส่วนขยาย Chrome (Chrome Extension) สำหรับช่วยตรวจสอบและแจ้งเตือนโครงการวิจัยที่รอการพิจารณา (Proposal List Agree) บนระบบ NRIIS (nriis.go.th) โดยอัตโนมัติ

## ✨ ความสามารถหลัก (Features)

*   **ระบบตรวจสอบอัตโนมัติ (Auto Check):** ทำงานเบื้องหลัง (Background Service Worker) เพื่อตรวจสอบรายการโครงการใหม่ทุกๆ 60 นาที
*   **เข้าสู่ระบบอัตโนมัติ (Auto Login):** หาก Session หมดอายุ ระบบจะทำการ Login ใหม่อัตโนมัติโดยใช้รหัสผ่านที่บันทึกไว้
*   **การแจ้งเตือน (Notifications):**
    *   แสดงตัวเลขจำนวนโครงการที่ไอคอน (Badge)
    *   แจ้งเตือนผ่าน Chrome Notification เมื่อพบโครงการใหม่
    *   ส่งข้อมูลไปยัง Google Apps Script (เพื่อแจ้งเตือนผ่าน LINE หรือเก็บลง Google Sheets)
*   **ดึงข้อมูลละเอียด (Data Scraping):** ดึงข้อมูลสำคัญ เช่น รหัสโครงการ, ชื่อโครงการ, นักวิจัย, คณะ, งบประมาณ, แหล่งทุน และสถานะ
*   **หน้าตั้งค่า (Options Page):** สำหรับบันทึก Username และ Password ของระบบ NRIIS อย่างปลอดภัย (เก็บใน Chrome Storage Sync)

## 📦 การติดตั้ง (Installation)

เนื่องจากส่วนขยายนี้ไม่ได้อยู่บน Chrome Web Store คุณต้องติดตั้งแบบ "Unpacked" (สำหรับนักพัฒนา) ดังนี้:

1.  **ดาวน์โหลด Source Code:**
    *   Clone repository นี้ หรือดาวน์โหลดเป็น ZIP แล้วแตกไฟล์ออกมา
    *   `git clone https://github.com/omaga03/autoNRIISOnProposalListAgree.git`

2.  **เปิดหน้าจัดการส่วนขยายใน Chrome:**
    *   เปิด Google Chrome
    *   ไปที่ URL: `chrome://extensions/`
    *   หรือกดที่เมนู 3 จุด (ขวาบน) -> Extensions -> Manage Extensions

3.  **เปิด Developer Mode:**
    *   ที่มุมขวาบนของหน้า Extensions ให้เปิดสวิตช์ **"Developer mode" (โหมดนักพัฒนาซอฟต์แวร์)**

4.  **โหลดส่วนขยาย:**
    *   คลิกปุ่ม **"Load unpacked" (โหลดส่วนขยายที่ยังไม่ได้แพ็ก)** ที่ปรากฏขึ้นมาทางซ้ายบน
    *   เลือกโฟลเดอร์ของโปรเจกต์นี้ (โฟลเดอร์ที่มีไฟล์ `manifest.json`)

## ⚙️ การตั้งค่า (Configuration)

หลังจากติดตั้งเสร็จแล้ว จำเป็นต้องตั้งค่ารหัสผ่านเพื่อให้ระบบ Auto Login ทำงานได้:

1.  คลิกขวาที่ไอคอนของส่วนขยาย (รูปโลโก้ NRIIS) บนแถบเครื่องมือ Chrome
2.  เลือกเมนู **"Options" (ตัวเลือก)**
3.  ในหน้าตั้งค่า:
    *   กรอก **ชื่อผู้ใช้ (Username)** ของระบบ NRIIS
    *   กรอก **รหัสผ่าน (Password)**
    *   กดปุ่ม **"บันทึกข้อมูล"**
4.  ระบบจะเก็บข้อมูลของคุณไว้ในเครื่อง (Chrome Storage) เพื่อใช้ในการ Login เท่านั้น

## 🚀 การใช้งาน (Usage)

*   **การทำงานเบื้องหลัง:** เมื่อติดตั้งและตั้งค่าเสร็จแล้ว ส่วนขยายจะทำงานเองโดยอัตโนมัติ คุณไม่ต้องเปิดหน้าเว็บ NRIIS ค้างไว้
*   **ดูสถานะ:** คลิกที่ไอคอนส่วนขยาย (Popup) เพื่อดูจำนวนโครงการที่รอพิจารณาล่าสุด และสถานะ Session ID
*   **ลิงก์ด่วน:** ในหน้า Popup จะมีลิงก์สำหรับเปิดหน้า "ตรวจสอบ/รับรองทุนภายนอก" และลิงก์ไปยังไฟล์ Excel (Google Sheets) ที่เชื่อมต่อไว้

## 📂 โครงสร้างไฟล์ (File Structure)

*   `manifest.json`: ไฟล์กำหนดค่าหลักของ Extension (Manifest V3)
*   `js/background.js`: Script หลักที่ทำงานเบื้องหลัง (Service Worker) ควบคุม Logic การตรวจสอบ, Login และแจ้งเตือน
*   `js/autoNRIISOnProposalListAgree.js`: Content Script (ปัจจุบันยังไม่ได้ใช้งานหลัก เน้นใช้ DOM Scripting ผ่าน background)
*   `popup.html` & `js/popup.js`: หน้าต่างเล็กๆ ที่เด้งขึ้นมาเมื่อกดไอคอน
*   `options.html` & `js/options.js`: หน้าตั้งค่า Username/Password
*   `js/images/`: โฟลเดอร์เก็บรูปภาพไอคอนต่างๆ

## ⚠️ ข้อควรระวัง (Disclaimer)

*   ส่วนขยายนี้พัฒนาขึ้นเพื่ออำนวยความสะดวกในการตรวจสอบข้อมูลเท่านั้น ไม่ใช่ซอฟต์แวร์อย่างเป็นทางการจาก NRIIS
*   ข้อมูลรหัสผ่านของคุณถูกเก็บไว้ใน Browser ของคุณเอง (Chrome Storage Sync) ผู้พัฒนาไม่มีส่วนรู้เห็นกับข้อมูลดังกล่าว
*   การใช้งานระบบอัตโนมัติอาจขึ้นอยู่กับนโยบายของเว็บไซต์ปลายทาง

---
**Developed by:** Omaga03
